class: Grid
attributes:
cases (list of Case)

methods:
draw
reveal_case(index)
reveal_all
hide_all

note: should we have the size of the window as an attribute to make it easier to draw the whole grid?

___

class: Case
attributes:
target (bool)
currently_revealed (bool)

methods:
draw_at(window_coordinates)

___

class: GameState
attributes:
watch_time (int or float)
remaining_tries (int)
score (int)
grid (Grid)

methods:
draw
mouse_at(window_coordinates)
click_at(window_coordinates)
start_reveal
end_reveal
update
